[project]
name = "everywear-whisper-align"
version = "0.1.0"
description = "Gener8 whisper forced-alignment sidecar"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2",
  "stable-ts>=2.17",
]

[project.scripts]
serve = "server:main"

[tool.uv]
package = true
