# Production: assets are local-first. The Everywear shell build copies
# public/* into everywear-os/dist/cs-assets, and the shell sets
# window.__EVERYWEAR_ASSET_BASE__ = "/cs-assets" before mounting Avatar Studio.
# Do not set this to a runtime CDN/R2 URL.
VITE_ASSET_PATH=
