# Dependencies
node_modules

# Build output
dist

# Served assets: local for dev and production bundle. Never commit binaries.
public/*
!public/manifest.json
!public/README.md

# Vite temp / alternate build output (sandbox artifacts)
dist2
vite.config.ts.timestamp-*.mjs
