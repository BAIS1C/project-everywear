/**
 * mint-utils.js — STUBBED FOR DEMO
 * All blockchain/wallet functions are no-ops.
 * Original implementation preserved in git history.
 * Will be replaced with TON integration later.
 */

export const connectWallet = async (network) => {
  console.warn("[mint-utils] connectWallet stubbed — blockchain disabled for demo")
  return null
}

export const mintAsset = async () => {
  console.warn("[mint-utils] mintAsset stubbed — blockchain disabled for demo")
  return null
}

export const fetchOwnedNFTs = async () => {
  console.warn("[mint-utils] fetchOwnedNFTs stubbed — blockchain disabled for demo")
  return []
}

export const ownsCollection = async () => {
  console.warn("[mint-utils] ownsCollection stubbed — blockchain disabled for demo")
  return false
}

export const buySolanaPurchasableAssets = async () => {
  console.warn("[mint-utils] buySolanaPurchasableAssets stubbed — blockchain disabled for demo")
  return null
}

export const getTokenPrice = async () => {
  console.warn("[mint-utils] getTokenPrice stubbed — blockchain disabled for demo")
  return 0
}

export const fetchSolanaPurchasedAssets = async () => {
  console.warn("[mint-utils] fetchSolanaPurchasedAssets stubbed — blockchain disabled for demo")
  return []
}
