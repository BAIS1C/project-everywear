# Character Studio is mounted inline by the shell Vite app, so production shell
# builds must provide the same public asset base as the applet package.
VITE_ASSET_PATH=https://assets.everywear.id/character-studio
