
---

## Current State Addendum 2026-05-30T10:25+08: Post-Extraction Runtime QA — New Carries

Location: C:\Users\MAG MSI\Project Everywear
Pass type: live runtime QA against `target/release/everywear-os.exe` after the 2026-05-29 Gener8 Pro capability extraction. Operator: Sean. New bugs to address in the next session.

### Bug 1: Folder panel occlusion at narrow window widths

Symptom: in the small (narrow) shell window frame, the desktop applet folder panel (top group containing 1M / G8 / VD / AD / 3N) does not occlude the underlying desktop icon column. The Vault (VA) icon visually bleeds through the folder panel where they overlap. In the full-size shell window the layout has enough horizontal room that the folder panel and the icon column do not collide; no overlap is visible.

Diagnosis hypothesis (to verify, do not assume): folder panel container probably uses a transparent or semi-transparent background and relies on layout separation rather than z-index + opaque/blur backdrop. At narrow widths the icon column reflows under the folder panel anchor point and visually shows through.

Likely surface: `platform/everywear-os/src/styles/shell.css` (2,262 lines, already a soft-target breach on the OODA punch list) + the folder panel component (find via grep on the desktop icon group rendering).

Required fix shape: solid or backdrop-filter blur background on the folder panel when expanded, with explicit z-index above the desktop icon column. Do NOT add absolute positioning hacks that break the full-size layout.

### Bug 2: Gener8 player does not work in current build

Symptom: after the 2026-05-29 Pro audio capability extraction, the Gener8 in-applet audio player does not play.

Hypotheses (rank by likelihood, verify each on next session):

1. **Audio ref/state moved into the Pro module and the free-tier player path lost its wiring.** During extraction, `referenceAudioUrl` / `sourceAudioUrl` / `coverSong` and their refs moved into `useProAudioMode` and `ProAudioModePanel`. If the Song-mode player relied on any of those refs or shared a `<audio>` element with the now-Pro-only code, the Song-mode playback path is broken.
2. **Tauri asset protocol scope drift.** The vault file URL resolution path (`convertFileSrc` in `packages/transport/src/vault.ts`) requires `$HOME/Documents/Everywear Vault/**` in the `tauri.conf.json` scopes. If that scope wasn't carried forward correctly after the Library route change, vault audio returns 403.
3. **New `views/LibraryView.tsx` (354 lines) doesn't wire its player to the same `<audio>` element / state as the legacy `components/LibraryView.tsx` did.** Codex switched the active route as part of the extraction PR but the player integration may have regressed.
4. **`SongStoreContext` wiring change.** Codex's diff touched context boundaries; the song store consumer chain may have changed.

Required next-session step: instrument the Song-mode play button click handler, trace from React click → state update → `<audio>` `src` assignment → asset protocol resolution. Identify the broken link before patching.

### Bug 3: Pro extraction shell-side incomplete (predicted, now confirmed live)

Sean's read after running everywear-os.exe: "the split clearly has not happened properly."

Interpretation: the gener8-applet-local `entitlementResolved` flag is in place and works for the gener8 React tree. But when the gener8 applet runs INSIDE the everywear-os shell, the shell-side AuthContext (at `platform/everywear-os/src/shell/AuthContext.tsx`, untouched by Codex) still races, and the gener8 applet's auth state depends on the shell's resolve sequence reaching it. The bounce or mount/unmount churn surfaces at the shell boundary, not the applet boundary.

This was flagged in the 2026-05-29T10:55+08 addendum as the shell-side prerequisite. Now confirmed live. The shell AuthContext needs its own `entitlementResolved` flag, propagated through whatever IPC pipes auth state into the gener8 applet at boot.

### New carries for next session

- **B1: Folder panel occlusion at narrow widths.** Fix at shell.css + folder panel component. Verify against both narrow and full-size window frames before commit.
- **B2: Gener8 player broken.** Trace from click handler through `<audio>` element src to Tauri asset protocol. Likely regression from extraction PR scope creep on the Library route. Do NOT assume it's the audio refs; instrument first.
- **B3: Shell-side `entitlementResolved` flag.** Add to `platform/everywear-os/src/shell/AuthContext.tsx`. Propagate to applets via the auth IPC contract. This is the prerequisite for Vid Pro and AI Director planner capability extractions and the path to retiring the 2026-05-28 release blocker local owner bypass.

### Open carries from prior addendums (still standing)

- AuthContext.tsx (shell) local owner bypass: release blocker.
- Runtime tests gap from the 2026-05-29 Codex extraction PR: `useProAudioMode`, `ProVaultPicker`, `entitlementGate` all have type-tests only.
- Vault build verification skipped by Codex: full `npm run build --workspace`, `cargo check -p gener8`, `cargo check -p everywear-os` were owed; Sean ran these manually during the 2026-05-30T02:19+08 release build.
- AI Director SAPI adapter unplumbed; Gener8 shim returns fallback shot plans.
- Tauri native launch gating uses tier rank, not neutral entitlement flags.
- Supabase local verification blocked, no Docker on host.
- VideoGeneratorModal (4,374 lines) hard-ceiling violation; dedicated pass still owed.
- Dead code in `applets/gener8/web/src/components/LibraryView.tsx` after Library route change.
- CreatePanel still 121 lines over 2k soft target after extraction.

### Session wrap

Sean: started fresh context for the B1/B2/B3 fixes. This addendum is the handoff. Next session opener should read this addendum and CONTEXT.md section 7 vault boundary, then propose B2 instrumentation first (player is the most user-visible regression).
